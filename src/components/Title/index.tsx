import React from "react";
import { View, Text } from "react-native"
import Styles from "./styles";

const Title = () => {
  return (
    <Text style={Styles.title}>Memorizer</Text>
  )
}

export default Title
