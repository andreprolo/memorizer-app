import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
  title: {
    fontSize: 32,
    marginTop: 25,
    fontWeight: 'bold',
    textAlign: 'center',
  },
})

export default Styles
