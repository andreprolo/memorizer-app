import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
  timer: {
    fontSize: 48,
    marginTop: 25,
    textAlign: 'center',
  },
  disabled: {
    color: 'grey',
    borderColor: 'grey'
  },
})

export default Styles;
